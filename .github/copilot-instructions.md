Following are the user requirements narrated by user to AI for advice over the time during development. Use these to understand the user's needs and the context of the project. These points may not be exhaustive but are the ones that user has mentioned during the conversation. Use it as guidnace to the objectives of the project and the code development. 

- I want to make similar python program where by giving image, program will give me the code from image

- I tried with offline OCR modules like cv2, easyocr, pytesseract but they are not giving me the expected output. Thus, I tried LLM to extract code from image. Since, local LLM from Ollama was taking lot of time, I used Ollama cloud LLM

- what I want - language and format identification - if its ipynb file, create same. ipynb photo can have outputs visible in it, those need not be present in final code. if there are other such formats then take care of those. identify languages and make final code in that language extension.

- Let's move to next part. Now here is what I want. Say there is a code which is more than 500 lines. A typical photo of a code can contain around 50-100 lines of code depending on font and screen resolution. In that case user will have to take multiple photos of same script/code, so that all photos contain the whole code. so far we have tested with single image, which is giving accurate enough code file. In reality, I will have multiple photos and I will have to create single file from those photos. Here is issue - in practical application, we will give option to upload photos and application will generate one code file. In single turn, only one output file can be generated. (we can have bulk operation in later phases where input will be like array of array of photos. but that's later. for now single operation for practicality. tell your opinion on strategy/approach). Now for these multple photos uploaded, the ordering uploaded on server or app can be different that what user might have intended. i.e. image 'abc' might be first code image, but in my list/array it would be 4th. So correct ordering is one issue. There are three ways to solve this (you can suggest your solutions) - one is take help of line numbers from IDE to identify the all code lines. Its easy if line numbers are available. second solution is where if line numbers are not available, identify same code lines appearing at end of one photo and start of another photo -> these would be one after other images. But this is complicated solution. third solution is UI engineering side i.e. ask user the order of photos and give ability to arrange. Second issue is once ordering is settled, identify same lines at end of one photo and start of next photo and deduplicate it. if line numbers are present, it would be easier. if not, then we need to think how to do it. Third issue is reconstruction of code. actually above issue is part of this. additionally we can have case where single notebook cell is split between two photos. In resultant code, we would need to put the code in single cell so that it remains accurate. Don't give the codes/prompts yet. think it through first in step by step manner on how to handle these. Think iteratively multiple times if needed to arrive at solutions/suggestions. Tell me what can be done at our side (engineering) and what can be done at LLM side (prompting). Tell me if you have any alternate better solutions. Let's weigh pros and cos of all possible solutions.